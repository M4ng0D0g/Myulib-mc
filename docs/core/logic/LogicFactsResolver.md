# Legacy logic facts note
The current tree does not expose a standalone facts-resolver system.
## Use these current systems instead
- `GameInstance` and `GameData` for game state
- `TimerManager` and `TimerModels` for timer state
- `ServerEventBus` for shared events
## Notes
- Keep this page only as a compatibility placeholder.
