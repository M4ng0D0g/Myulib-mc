# Legacy logic actions note
The current tree does not expose a standalone actions system.
## Use these current systems instead
- `GameInstance` for state mutation
- `GameDefinition` for lifecycle wiring
- `TimerManager` for timer callbacks
- `ServerEventBus` for event dispatch
## Notes
- Keep this page only as a compatibility placeholder.
