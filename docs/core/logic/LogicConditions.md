# Legacy logic conditions note
The current tree does not expose a standalone conditions system.
## Use these current systems instead
- `GameStateMachine` for state flow
- `TimerManager` for time-based checks
- `ServerEventBus` for event-driven responses
- `PermissionManager` for access checks
## Notes
- Keep this page only as a compatibility placeholder.
