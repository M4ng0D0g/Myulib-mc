# Legacy logic signals note
The current tree does not expose a standalone signals system.
## Use these current systems instead
- `GameStateChangeEvent`
- `TimerEvents`
- `ServerEventBus`
## Notes
- Keep this page only as a compatibility placeholder.
