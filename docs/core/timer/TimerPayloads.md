# Timer payload namespace
`TimerPayloads` currently acts as a namespace placeholder.
## Current payload types
- `RespawnTimerPayload`
- `SoundTimerPayload`
## Notes
- New payload records should be added to `TimerModels` unless the design changes.
